<odoo>
    <data>

        <!-- ========== PROJECT TASK - KANBAN VIEW ========== -->
        <record id="view_university_task_kanban_custom" model="ir.ui.view">
            <field name="name">project.task.kanban.university</field>
            <field name="model">project.task</field>
            <field name="arch" type="xml">
                <kanban default_group_by="stage_id" class="o_kanban_small_column" records_draggable="true">
                    <field name="stage_id"/>
                    <field name="color"/>
                    <field name="priority"/>
                    <field name="user_ids"/>
                    <field name="name"/>
                    <templates>
                        <t t-name="kanban-box">
                            <div t-attf-class="#{kanban_color(record.color.raw_value)} oe_kanban_global_click">
                                <div class="oe_kanban_content">
                                    <div class="o_kanban_record_top">
                                        <strong class="o_kanban_record_title">
                                            <field name="name"/>
                                        </strong>
                                    </div>
                                    <div class="o_kanban_record_body">
                                        <field name="tag_ids" widget="many2many_tags"/>
                                    </div>
                                    <div class="o_kanban_record_bottom">
                                        <div class="oe_kanban_bottom_left">
                                            <field name="priority" widget="priority"/>
                                        </div>
                                        <div class="oe_kanban_bottom_right">
                                            <field name="user_ids" widget="many2many_avatar_user"/>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
        </record>


        <!-- ========== PROJECT TASK - FORM VIEW ========== -->

        <record id="view_university_task_form_custom" model="ir.ui.view">
            <field name="name">project.task.form.university</field>
            <field name="model">project.task</field>
            <field name="arch" type="xml">
                <form string="Task">
                    <header>
                        <field name="stage_id" widget="statusbar" options="{'clickable': '1'}"/>
                    </header>
                    <sheet>
                        <div class="oe_title">
                            <label for="name" string="Task Title"/>
                            <h1>
                                <field name="name" placeholder="Например: Анализ требований проекта"/>
                            </h1>
                        </div>
                        <group>
                            <group>
                                <field name="project_id" readonly="1"/>
                                <field name="user_ids" widget="many2many_avatar_user" domain="[('id', 'in', project_member_user_ids)]"/>
                                <field name="project_member_user_ids" invisible="1"/>
                            </group>
                            <group>
                                <field name="date_deadline"/>
                                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                            </group>
                        </group>
                        <notebook>
                            <page string="Description" name="description">
                                <field name="description" placeholder="Детально опишите задачу..."/>
                            </page>
                        </notebook>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids"/>
                        <field name="message_ids"/>
                    </div>
                </form>
            </field>
        </record>

    </data>
</odoo>