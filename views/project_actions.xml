<odoo>
    <record id="project_project_action_university" model="ir.actions.act_window">
        <field name="name">Projects</field>
        <field name="res_model">project.project</field>
        <field name="view_mode">tree,form</field>
        <field name="view_ids" eval="[(5, 0, 0),
            (0, 0, {'view_mode': 'tree', 'view_id': ref('project.view_project_tree')}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('project.edit_project')}),
            (0, 0, {'view_mode': 'kanban', 'view_id': ref('project.view_project_kanban')}),
        ]"/>
    </record>

    <menuitem 
        id="project.menu_main_pm_config" 
        name="Project" 
        action="project_managment.project_project_action_university" 
        sequence="10"/>

    <!-- Изменяем action для создания проекта -->
    <record id="open_create_project" model="ir.actions.act_window">
        <field name="name">Create a Project</field>
        <field name="res_model">project.project</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="project.edit_project"/>
        <field name="target">new</field>
        <field name="context">{"default_allow_billable": 0}</field>
    </record>
</odoo>